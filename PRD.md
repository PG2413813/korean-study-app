# 韩语学习记录 App（本地版）产品需求文档（PRD）

> 版本：v1.0（第一版）　状态：已确认定稿　定稿日期：2026-09-21
> 本文档汇总已确认的产品范围与设计决策，作为后续开发的唯一依据。开发过程中若与本文档冲突，以本文档为准；需调整时先改本文档再开发。

## 已确认的关键决策（记录）



* **运行形态**：单文件本地网页应用，只在本人电脑浏览器中运行；数据保存在本机浏览器，刷新、关闭、重启后不丢失；不需要登录、联网、云数据库、部署和手机端。

* **韩语输入**：电脑已安装韩语输入法，「给中文写韩语」采用打字判题，两种复习模式均可输入作答。

* **复习调度**：滚动复习全部旧词（详见 §8.4）。

* **判题标准**：忽略输入首尾空格，其余精确匹配；第一版不做助词差异等容错。

* **删除**：单词 / 错题删除需二次确认。

* **重复录入**：录入已存在单词时提示「已存在」，可选择跳过或合并。



***

## 1. 产品目标和使用场景

### 1.1 产品目标

为个人韩语学习提供一款**只在本地浏览器运行**的单词记录与复习工具，替代纸质笔记 / 表格记词，用「录入 → 每日复习 → 错题反复清」的闭环持续巩固记忆。

### 1.2 使用场景



* 每天学习新单词后，把「韩语单词 + 中文翻译」录入 App，自动归档进单词库。

* 每天打开 App 从首页快速开始当日复习：给中文写韩语，或给韩语写中文。

* 答错的单词自动进入错题库，后续复习中随机复现；连续答对满 3 次（次数可在设置中修改）自动移出错题库，回归普通词库。

* 定期导出备份文件存到电脑其他位置，防止浏览器本地数据意外清空。

* 偶尔打开错题库单独刷错题、检查某个单词的连续答对进度。

### 1.3 非目标（第一版明确不做）

发音 / 朗读、记忆曲线排期、例句与词性变化、复杂统计图表、手机端、云同步、部署、登录、联网。



***

## 2. 整体页面框架和导航结构



* **形态**：单页应用（一个 HTML 文件），无浏览器页面跳转。

* **布局**：左侧固定**侧边栏导航** + 右侧**主内容区**。主内容区随导航点击切换，显示对应模块页面。

* **导航共 6 项**（第 1 项为打开 App 的默认页）：



| 序号 | 模块     | 说明                |
| -- | ------ | ----------------- |
| 1  | 首页总览   | 默认页，学习状态摘要 + 快捷入口 |
| 2  | 单词库    | 全部单词的档案与检索        |
| 3  | 每日学习录入 | 每天新增单词            |
| 4  | 单词复习   | 核心模块，双模式复习与判题     |
| 5  | 错题库管理  | 答错单词集中管理          |
| 6  | 数据与设置  | 备份 / 恢复与偏好设置      |



* **数据流向**：任何页面发生的修改（录入、复习、编辑、删除、设置）均即时写入浏览器本地存储，关闭 / 重启后保持原样。



***

## 3. 首页的内容结构

首页自上而下包含：



1. **统计卡片（一行 3 个）**

* 今日待复习：昨天及以前录入、今天尚未完成复习的普通单词数量（不含今天新录入、不含错题）。

* 今日新录入：今天录入的单词数量。

* 错题总数：当前错题库中的单词数量。

1. **快捷录入框**：韩语单词输入 + 中文释义输入 + 「添加」按钮。不跳页直接加词，与「每日学习录入」页共用同一套规则（重复提示、自动记日期等）。

2. **快捷入口按钮**：

* 「开始今日复习」：跳转单词复习模块并直接开始当日复习会话。

* 「打开错题库」：跳转错题库管理模块。

1. **底部统计**：累计单词总数、连续学习天数（当天有录入或完成过复习动作即算当天学习；连续学习天数 = 自最近一次有学习动作的日期起向前连续计算的天数）。



***

## 4. 每个模块的用途



| 模块     | 用途                                   |
| ------ | ------------------------------------ |
| 首页总览   | 不用进子页面就能看到今日学习状态，并快速加词、一键进入复习        |
| 单词库    | 集中管理全部已录入单词，支持查看、搜索、编辑、删除、打标签        |
| 每日学习录入 | 每天集中录入新单词，自动记日期，次日自动进入复习池            |
| 单词复习   | 用「给中文写韩语 / 给韩语写中文」两种模式练习，自动判题，答错进错题库 |
| 错题库管理  | 查看与清错题：显示连续答对进度，可手动移除 / 编辑，可开启只刷错题   |
| 数据与设置  | 数据导出备份 / 导入恢复；调整每轮题量、错题移出次数等         |



***

## 5. 每个模块第一版最必要的数据和操作

### 5.1 核心数据模型

**单词条目**（单词库中的一条记录）



| 字段     | 必填 | 说明                                    |
| ------ | -- | ------------------------------------- |
| id     | 是  | 唯一标识                                  |
| 韩语原文   | 是  | 如 공부하다                                |
| 中文释义   | 是  | 如 学习                                  |
| 标签     | 否  | 可多个，如 动词、名词、TOPIK6；用于筛选               |
| 录入日期   | 是  | 自动记录；今天录入的明天进复习池                      |
| 上次复习日期 | 否  | 用于「最久未复习」排序；空 = 从未复习过                 |
| 是否错题   | 是  | 是否在错题库中                               |
| 错题加入日期 | 否  | 进入错题库的时间                              |
| 连续答对计数 | 是  | 仅错题单词有效：答对 +1，答错清零；达到移出次数（默认 3）后移出错题库 |

**设置项**



| 字段   | 默认值 | 说明                  |
| ---- | --- | ------------------- |
| 每轮题量 | 20  | 每轮复习出题数，可修改         |
| 移出次数 | 3   | 错题连续答对多少次后移出错题库，可修改 |

**学习记录**（用于统计与连续学习天数）：日期、当日新录入数、当日复习完成情况等，实现时按需记录，保证首页统计可算、可复核。

### 5.2 各模块数据与操作



| 模块     | 必要数据                           | 必要操作                                              |
| ------ | ------------------------------ | ------------------------------------------------- |
| 首页总览   | 今日待复习数、今日新录入数、错题总数、累计词数、连续学习天数 | 快捷录入添加；点击开始今日复习；点击打开错题库                           |
| 单词库    | 全部单词条目（韩语、中文、标签、录入日期）          | 列表查看；按韩语 / 中文关键词搜索；按标签筛选；编辑；删除（二次确认）              |
| 每日学习录入 | 韩语原文、中文释义、可选标签、录入日期            | 逐条添加（可连续添加）；查看今日已录列表；删除今日已录条目                     |
| 单词复习   | 复习模式、每轮题量、出题队列、判题结果            | 选择模式（中→韩 / 韩→中）；出题；输入作答并提交；判对错并反馈；显示正确答案；下一题；查看小结 |
| 错题库管理  | 错题单词 + 连续答对计数 + 错题加入日期         | 列表查看；编辑；手动移除；开启 / 关闭「只刷错题」                        |
| 数据与设置  | 备份文件、设置项                       | 导出备份；导入恢复；修改每轮题量；修改移出次数；恢复默认设置                    |



***

## 6. 首页和各模块之间的关系



* 首页的三个统计卡片分别由单词库（今日新录入、累计词数）、复习状态（今日待复习）、错题库（错题总数）聚合而来。

* 「快捷录入」写入单词库，效果与「每日学习录入」页添加完全一致（同一套规则）。

* 「开始今日复习」跳转单词复习模块并直接启动当日复习会话；复习产生的答错单词自动写入错题库，复习进度回写（影响首页「今日待复习」与连续学习天数）。

* 「打开错题库」跳转错题库管理模块。

* 连续学习天数由学习记录计算：当天有录入或完成复习动作即算当天学习。

* 各模块之间不互相依赖跳转（除首页快捷入口外），均通过统一的本地存储共享数据。



***

## 7. 本地数据文件、备份和恢复要求



1. **本地存储**：所有数据保存在当前电脑的浏览器本地存储中；刷新页面、关闭浏览器、重启电脑后数据不丢失。

2. **导出备份**：把全部数据（单词库、错题库、设置项、学习记录）导出为一个通用格式的备份文件（如 JSON），存到用户指定位置。

3. **导入恢复**：从备份文件完整恢复全部数据；导入前提示将覆盖当前数据，需用户确认。

4. **备份文件要求**：通用格式、可随时导回；同版本备份文件之间可互导。

5. **数据安全**：应用不需要联网即可完成全部功能；不向任何外部服务器上传数据。



***

## 8. 第一版必须完成的功能

### 8.1 首页总览



* 三个统计卡片：今日待复习、今日新录入、错题总数。

* 快捷录入框（韩语 + 中文 + 添加），复用录入规则。

* 快捷入口：「开始今日复习」「打开错题库」。

* 累计单词数、连续学习天数。

### 8.2 单词库



* 全部单词列表：韩语原文、中文释义、标签、录入日期。

* 搜索：按韩语或中文关键词查找。

* 标签：可添加 / 编辑标签；按标签筛选列表。

* 编辑单词（改韩语 / 中文 / 标签）；删除单词（二次确认）。

### 8.3 每日学习录入



* 输入韩语单词 + 中文释义（标签选填），逐条添加，支持连续录入。

* 自动记录录入日期；今天录入的单词明天自动进入待复习池。

* 显示今日已录清单，可删除单条。

### 8.4 单词复习（核心）



* **两种模式随时切换**：给中文写韩语（中→韩）、给韩语写中文（韩→中）。

* **出题来源（已确认的滚动调度）**：

1. 主队列：昨天及以前录入、今天尚未复习的单词，按「最久未复习」优先出题（从未复习过的排最前）。

2. 随机混入：错题库单词在每轮复习中随机插入，保证「随机出现」。

3. 今天新录入的单词今天不出现；同一单词同一轮不重复出现；答对的普通单词当天不再出现，第二天重新进入池子继续滚动。

* **判题**：忽略输入首尾空格，其余精确匹配；判对绿色提示，判错红色提示并显示正确答案。

* **答错自动进错题库**（若已在错题库则保持不变，答错清零计数）。

* **错题规则**：错题单词答对 → 连续答对计数 +1；答错 → 计数清零；连续答对达到移出次数（默认 3）→ 自动移出错题库，回归普通词库，不再作为错题出现。

* **小结**：每轮结束显示总题数、答对数、新增错题数；可「再刷一轮」或返回。

### 8.5 错题库管理



* 错题列表：单词、中文释义、连续答对计数（如 1/3）、加入日期。

* 编辑错题释义；手动移除（二次确认）。

* 「只刷错题」开关：开启后复习只从错题库出题。

### 8.6 数据与设置



* 导出备份、导入恢复（见 §7）。

* 设置：每轮题量（默认 20）、错题移出次数（默认 3），修改后立即生效并持久化。

* 恢复默认设置。

### 8.7 基础规则（贯穿各模块）



* 重复录入提示：录入已存在单词时提示「已存在」，可选择跳过或合并。

* 删除二次确认。

* 全部数据本地持久化。



***

## 9. 第一版暂时不做的功能



* 单词发音、韩语朗读。

* 艾宾浩斯记忆曲线等复杂复习排期（先使用已确认的滚动调度 + 错题规则，后续需要再评估）。

* 例句、词性变化、派生词等拓展字段（动词 / 名词等需求由标签覆盖）。

* 学习趋势图、长期统计图表等复杂可视化。

* 助词差异（如 은/는）等宽松判题容错。

* 多设备同步、云数据库、账号登录、联网功能、部署上线、手机端适配。



***

## 10. 可以实际检查的验收标准

> 以下每一条均可直接打开应用实际操作验证。

**导航与框架**



1. 打开应用后左侧显示 6 项导航，默认进入「首页总览」；点击各项可正确切换页面，无浏览器跳转。

2. 整个应用断网环境下可正常使用全部功能。

**录入与单词库**

3\. 在「每日学习录入」添加单词后：单词库立即可见该词；首页「今日新录入」+1；今日已录清单显示该条。

4\. 首页快捷录入添加单词后，效果与录入页完全一致。

5\. 录入一个已存在的单词（韩语相同）时出现「已存在」提示，可跳过或合并。

6\. 在单词库按韩语或中文关键词搜索，能准确找到对应单词；按标签筛选生效。

7\. 编辑单词后列表内容更新；删除单词弹二次确认，确认后从列表移除。

**复习与判题**

8\. 今天录入的单词今天不出现；昨天及以前录入的单词会出现。

9\. 中→韩、韩→中两种模式均可正常出题、输入、判题。

10\. 输入内容首尾带空格仍判为正确；拼写不一致判为错误并显示正确答案。

11\. 答对的普通单词当天不再重复出现；第二天重新进入复习池。

12\. 错题库中的单词会在复习中随机出现。

**错题规则**

13\. 答错单词自动进入错题库，列表可见，连续答对计数为 0。

14\. 错题单词答对一次，计数 +1；再答错，计数清零（界面数字正确反映）。

15\. 错题连续答对满 3 次（默认）后自动移出错题库，回归普通词库，此后不再作为错题出现。

16\. 将「移出次数」改为 2 后，错题连续答对 2 次即移出；改回 3 后按 3 次执行。

17\. 开启「只刷错题」后，一轮复习只出错误题；关闭后恢复正常混合出题。

18\. 每轮复习结束显示小结：总题数、答对数、新增错题数，数字与实际作答一致。

**设置与数据**

19\. 修改「每轮题量」后，下一轮复习按新题量出题；「恢复默认设置」后回到 20 / 3。

20\. 录入、复习、编辑、设置后刷新页面，再关闭浏览器重新打开，所有数据保持原样。

21\. 导出备份生成一个文件；删除若干数据后导入该备份文件，数据完整恢复（单词、错题、设置、学习记录）。

22\. 首页三个统计卡（今日待复习、今日新录入、错题总数）、累计单词数、连续学习天数与实际数据一致。