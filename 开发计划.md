# 韩语学习记录 App 开发计划（第一版）

> 依据：`PRD.md` v1.0（2026-09-21 定稿）　编制日期：2026-09-21
> 本计划可直接执行：每个阶段都有明确产出与验证方式；核心算法可直接照 §2 实现；验收按 §4 逐条实测。

---

## 0. 总体方案（技术选型）

| 项目 | 选型 | 理由 |
| --- | --- | --- |
| 交付形态 | 单个自包含 HTML 文件 `韩语学习记录.html` | PRD §2/§7：单文件本地应用；双击即用，无需安装 |
| 技术栈 | 原生 HTML + CSS + JavaScript（无框架、无构建、零外部依赖） | 断网可用（验收 2）、易维护、单文件体积小 |
| 存储 | 浏览器 localStorage，单一 key `hanStudyApp.v1`（含 schema 版本号） | 个人词库规模（数千词远小于约 5MB 上限）足够，实现最简 |
| 备份/恢复 | 导出 / 导入 JSON 文件（Blob 下载、file input 读取） | PRD §7 |
| 韩文显示 | 系统字体，Windows 韩文字体（Malgun Gothic）兜底 | 离线正确显示韩文 |
| 运行与验证 | 本机浏览器直接打开文件；验收用真实浏览器操作走查 + 检查 localStorage | 无需服务器 |

关键取舍：
- 不引入任何外部库/CDN，保证完全离线（PRD §10 验收 2）。
- 数据每次变更后立即原子写入 localStorage（单 key 整体覆盖），防刷新丢失（验收 20）。

---

## 1. 文件与代码结构（单文件内分区）

`韩语学习记录.html` 内部按顺序分区，每区用注释标记：

1. `<style>` 样式区：整体布局（左侧固定侧边栏 + 主内容区）、通用组件样式（卡片、按钮、输入框、弹窗、提示条、标签、统计卡）。
2. HTML 骨架：侧边栏导航（6 项）+ 6 个内容区 `<section>` + 全局弹窗/确认框容器。
3. JS 分区（IIFE，按职责划分，互不交叉）：
   - `STORE`：数据加载/保存/导出/导入/结构校验；
   - `DOMAIN`：日期与统计、复习队列构建、判题、错题迁移、连续学习天数、重复录入合并；
   - `UI`：6 个页面的渲染函数与事件绑定；
   - `BOOT`：初始化、导航路由切换、启动默认首页。

**本地存储格式**（localStorage key：`hanStudyApp.v1`）：

```json
{
  "version": 1,
  "settings": { "roundSize": 20, "removeAfter": 3 },
  "words": [
    {
      "id": "唯一id",
      "ko": "공부하다",
      "zh": "学习",
      "tags": ["动词"],
      "createdAt": "2026-09-21",
      "lastReviewedAt": null,
      "isError": false,
      "errorSince": null,
      "correctStreak": 0
    }
  ],
  "studyLog": { "2026-09-21": { "added": 5, "reviewed": 20 } }
}
```

字段含义见 PRD §5.1；`studyLog` 按日期记录当日录入数与复习完成数，用于首页统计与连续学习天数。

---

## 2. 核心算法（可直接照此实现）

### 2.1 日期
- 一律使用本地日期 `YYYY-MM-DD`（避免时区误差）；`today()` 为统一入口。

### 2.2 首页统计
- **今日新录入** = `studyLog[today].added`（无记录则为 0）。
- **今日待复习** = words 中满足「`createdAt < today` 且 `lastReviewedAt != today` 且 `isError == false`」的数量（不含今天新录、不含错题，符合 PRD §3）。
- **错题总数** = words 中 `isError == true` 的数量。
- **累计单词数** = `words.length`。
- **连续学习天数** = 从今天向前（若今天尚无学习动作，则从昨天向前），连续每天都有学习动作（added>0 或 reviewed>0）的天数。

### 2.3 复习队列构建（滚动调度，已确认方案）
输入：设置题量 `roundSize`。
1. **普通池 A** = words 中「`isError==false` 且 `createdAt < today` 且 `lastReviewedAt != today`」，按最久未复习优先排序：`lastReviewedAt` 为空（从未复习）排最前，其余按 `lastReviewedAt` 升序，同日按 `createdAt` 升序。
2. **错题池 B** = words 中 `isError==true`。错题**不受「当天已复习」限制**，任何当天复习会话都可再次出现（保证可反复刷、随机出现）。
3. 若 `|B| ≥ roundSize`：本轮只随机出 `roundSize` 道错题（随机打乱取前 `roundSize`）。
4. 否则：普通池取前 `roundSize − |B|` 道；将错题池 B 的单词逐个**随机插入**到普通队列的随机位置（不改变普通队列相对顺序，保证「最久未复习优先」与「错题随机出现」同时成立）。
5. 同一轮内不重复出题。

### 2.4 判题
- 输入与答案先 `trim`。
- 中文释义若含多个释义（以 `、`、`；`、`，`、`/` 分隔），**任一释义精确匹配即判对**——这是对 PRD「精确匹配」在"一词多义"场景的实现解释，开发时会向你确认。
- 其余情况精确匹配（不做助词差异等容错，PRD §9）。

### 2.5 错题迁移（状态机）
- **答错**（无论普通词还是错题）：`isError=true`、`errorSince=today`、`correctStreak=0`。
- **错题答对**：`correctStreak+1`；若 `correctStreak >= removeAfter`（默认 3）→ `isError=false`、`errorSince=null`、`correctStreak=0`（自动移出错题库、回归普通词库，PRD §8.4）。
- **普通词答对**：不动错题相关字段。

### 2.6 复习进度与小结
- 每答一题更新 `lastReviewedAt=today`（普通词当天不再出现；错题按 2.3 不受此限制）。
- 一轮结束生成小结：**总题数 / 答对数 / 新增错题数**（新增错题 = 本轮由非错题变为错题的单词数）。
- 学习动作回写 `studyLog[today].reviewed`。

### 2.7 重复录入
- 录入时按韩语（trim 后）精确查找；已存在 → 提示「已存在」，提供「跳过 / 合并」。
- **合并语义**：中文释义不同则用 `、` 连接去重合并；标签去重合并；不动录入日期、复习与错题状态。

---

## 3. 分阶段实施步骤（每阶段有产出与验证）

### 阶段 0：工程骨架
- 产出：创建 `韩语学习记录.html`，完成 HTML 结构（侧边栏 6 项 + 6 个 section）、CSS 基础样式、JS 空壳与本地存储初始化。
- 验证：浏览器打开显示 6 项导航并可切换（页面为占位内容）；localStorage 出现初始数据。

### 阶段 1：数据层（STORE）
- 产出：load/save、默认数据、导出 JSON、导入 JSON（含版本与结构校验、覆盖前二次确认）。
- 验证：导出文件内容正确；导入损坏 JSON 会报错且不破坏现有数据；刷新后数据保留。

### 阶段 2：核心逻辑（DOMAIN）
- 产出：§2.1–§2.7 全部算法函数 + 纯数据测试用例。
- 验证：为每个算法准备 3–5 个用例（如：待复习计数口径、错题满 3 次移出、连续天数跨月/断档、随机混入不破坏优先级、多释义判题），用临时数据在浏览器 console 或 Node 直接调用核验结果。

### 阶段 3：页面与交互（UI）
按顺序实现并即时验证：
1. **首页总览**：统计卡、快捷录入、两个快捷按钮、累计统计。
2. **每日学习录入**：表单、连续录入、今日已录清单、删除、重复提示/合并。
3. **单词库**：列表、搜索、标签筛选、编辑弹窗、删除二次确认。
4. **单词复习**：开始页（模式/题量）→ 答题卡（出题/输入/提交/判题反馈/显示答案/下一题）→ 小结（总题数/答对数/新增错题 + 再刷一轮/返回）；两种模式可切换；「只刷错题」接入队列构建（2.3）。
5. **错题库管理**：列表（含连续答对进度如 1/3）、编辑、手动移除、只刷错题开关。
6. **数据与设置**：导出、导入、每轮题量、移出次数、恢复默认。
- 验证：每完成一个页面，立即在浏览器手动走查该页全部交互；跨页数据联动核对（如录入后首页「今日新录入」+1、词库立即可见）。

### 阶段 4：联调与持久化
- 产出：完整应用闭环。
- 验证：完整流程演练（录入 5 词 → 复习 → 故意答错 → 错题库出现 → 只刷错题 → 连续答对 3 次移出）；刷新页面、关闭浏览器重新打开数据不丢；断网打开正常。

### 阶段 5：验收走查
- 产出：对照 PRD §10 的 22 条验收标准逐条实测（真实浏览器操作 + 检查 localStorage 数据），全部通过后交付；未通过的现场修复后复测，并在交付说明中列出验证方式、覆盖范围与遗留缺口。

---

## 4. 验收对照表（22 条 ↔ 测试方法）

| PRD 验收条目 | 实测方法 |
| --- | --- |
| 1 导航与默认页 | 打开文件，点击 6 项导航逐一核对，默认进首页 |
| 2 断网可用 | 浏览器离线模式打开并完整使用一遍 |
| 3–4 录入联动 | 录入页与首页快捷录入各加 1 词，核对词库、首页数字、今日已录清单 |
| 5 重复提示 | 重复录入同一韩语词，核对「已存在」提示、跳过与合并效果 |
| 6 搜索/筛选 | 输入关键词、选标签，核对结果 |
| 7 编辑/删除 | 编辑后列表更新；删除弹二次确认，确认后移除 |
| 8–11 复习与判题 | 构造「今天录入/昨天录入」数据核对出题；双模式各答 3 题；首尾空格判对；拼写错误判错并显示答案；答对当天不再出现、次日再入池 |
| 12 错题随机出现 | 错题库放 3 词，连续 3 轮核对出现且位置随机 |
| 13–17 错题规则 | 答错进库计数 0；答对 +1、答错清零；满 3 次自动移出；改 2 次后按 2 次移出；只刷错题只出错题 |
| 18 小结 | 一轮 5 题（2 对 3 错）核对小结数字与作答一致 |
| 19 设置生效 | 题量改 5 后一轮 5 题；恢复默认回 20/3 |
| 20 持久化 | 操作后刷新、关闭重开，核对全部数据 |
| 21 备份/恢复 | 导出 → 删除部分单词 → 导入 → 核对完整恢复 |
| 22 首页统计 | 与词库/错题库/学习记录逐一核对一致 |

---

## 5. 风险与注意点

- **localStorage 容量**（约 5MB）：个人词库规模足够；定期导出备份可防意外清空。
- **单文件可维护性**：不引库，JS 按 STORE/DOMAIN/UI/BOOT 分区注释，控制单文件复杂度。
- **中文文件名与韩文输入**：Windows 双击打开正常；韩语输入由系统输入法完成，判题只比对文本。
- **导入覆盖**：恢复备份前必须二次确认（PRD §7）。
- **歧义处理**：开发中若遇到 PRD 未覆盖的歧义（如 §2.4 多释义判题），先向用户确认并更新 PRD/本计划，再继续实现，不静默自定。

---

## 6. 交付

- 唯一交付物：`韩语学习记录.html`
- 交付前完成阶段 5 全量验收；交付时说明：双击用浏览器打开、数据全在本机、建议定期在「数据与设置」页导出备份文件。
